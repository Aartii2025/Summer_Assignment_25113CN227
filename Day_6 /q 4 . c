#include <stdio.h>

int main() {
    int x , n , i , result = 1;
    printf("Enter the number");
    scanf("%d" , &x);
    printf("Enter the power");
    scanf("%d", &n);
    for (i = 1 ; i <= n ; i++){
        result = result * x ;
    }
printf("%d" , result);
    return 0;
}

#include <stdio.h>
int rev = 0;
int reverse(int n){
    if ( n == 0 )
    return rev;

    else 
    rev = rev*10 + (n % 10) ;
    reverse (n/10);
}
int main() {
int n ;
printf("Enter the number");
scanf("%d" , &n);
printf("Reverse is %d" , reverse(n));
    return 0;
}
